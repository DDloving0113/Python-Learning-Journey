- 每个练习文件以中文注释说明题目与行为规则，函数内部保留 TODO 提示引导实现。
- 所有可执行逻辑放在 `if __name__ == "__main__":` 块内，函数本身只 return 结果而不直接 print。
- 输入数据优先以模块级变量内联声明，避免硬编码路径，便于独立运行。
- 对异常输入采用防御式编程：除零检查、空字段跳过、len(parts)<4 等守卫条件。
- 字符串处理统一使用 strip() 去空白、split(": ") 或 split(",") 分割后再转换类型。