# ### 3）质数工具函数
# - 题目：  
#   在你之前的质数判断基础上升级：
#   1. 编写函数 `is_prime(n)`：  
#      - 非整数或小于 `2`，返回 `False`  
#      - 否则按常规方式判断是否为质数，返回 `True/False`
#   2. 编写函数 `primes_in_range(a, b)`：  
#      - 接受两个整数 `a, b`（允许 `a > b`）  
#      - 返回 `[min(a, b), max(a, b)]` 区间内所有质数组成的列表  
#   主程序中调用 `primes_in_range`，打印出返回的质数列表。

